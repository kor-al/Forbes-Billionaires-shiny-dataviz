window.addEventListener('load', function () {
  //console.log("Loaded")
})

//---Experimenting with Shiny Events---
//$(document).on('shiny:recalculated', function(event) {
//  if (event.target.id.includes("header")){
    //console.log(event.target)
//    event.target.style.opacity = 1
//  }
//});